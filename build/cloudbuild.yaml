steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: 'bash'
  args:
  - '-c'
  - gsutil -m rsync -x "^stripe_airflow\|^\.git" -d -r ./ gs://${_BUCKET_NAME}/stripe_airflow

substitutions:
  _BUCKET_NAME: europe-west1-stripe-airflow-2ff1403a-bucket
options:
  logging: CLOUD_LOGGING_ONLY
# gsutil -m rsync -x "^dags\|^\.git" -d -r . gs://${_BUCKET_NAME}/plugins
